import { styled } from '@/styles';

export const HeaderStyled = styled('header', {
  width: '100%',
  height: '45px',

  background: '$N1',
  boxShadow: '$default',

  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',

  marginBottom: '$48',
});
